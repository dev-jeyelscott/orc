// No domain tables yet. Later phases (agents, tasks, runs, executions,
// events, terminal history) add tables here as the runtime needs them —
// see AGENTS.md: "Add a boundary only when the implementation needs it."
export {};
