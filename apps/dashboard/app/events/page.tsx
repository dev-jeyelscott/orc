import { EventsList } from "@/components/events-list";

/**
 * Renders the operator-facing persisted domain-event history workspace.
 */
export default function EventsPage() {
  return (
    <EventsList />
  );
}
