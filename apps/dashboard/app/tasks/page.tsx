import {
  TasksManager,
} from "@/components/tasks-manager";

/** Renders the Tasks command center while the client manager owns live workflow state. */
export default function TasksPage() {
  return <TasksManager />;
}
